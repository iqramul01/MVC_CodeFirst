@model IEnumerable<_1263087.Models.Admit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <h3>
        {CRUD Operation Using Model, Required Validation, Generic Repository, Validation Summary, Computed Column.}
    </h3>
</p>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patient.PatientName)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.AdmitFee)*@
            Admit Fee
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.TotalBill)*@
            Total Bill
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Discount)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.NetBill)*@
            Net Bill
        </th>
        <th>
            Action
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.PatientName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdmitFee)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalBill)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Discount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NetBill)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdmitID },new { @class="btn btn-primary btn-sm"}) 
            @Html.ActionLink("Details", "Details", new { id=item.AdmitID },new { @class="btn btn-info btn-sm"}) 
            @Html.ActionLink("Delete", "Delete", new { id=item.AdmitID },new { @class="btn btn-danger btn-sm"})
        </td>
    </tr>
}

</table>
